<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="clickListener"
            type="android.view.View.OnClickListener" />

        <variable
            name="longClickListener"
            type="android.view.View.OnLongClickListener" />

        <variable
            name="position"
            type="Integer" />

        <variable
            name="viewModel"
            type="org.naminfo.activities.main.contact.viewmodels.ContactViewModel" />

        <variable
            name="selectionListViewModel"
            type="org.naminfo.activities.main.viewmodels.ListTopBarViewModel" />

        <variable
            name="contact"
            type="org.naminfo.activities.main.contact.data.PBXContactsTable" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="@{clickListener}"
        android:onLongClick="@{longClickListener}"
        android:padding="8dp">

        <!-- Profile Image -->
        <ImageView
            android:id="@+id/avatar"
            android:layout_width="@dimen/contact_avatar_size"
            android:layout_height="@dimen/contact_avatar_size"
            android:layout_centerVertical="true"
            android:layout_marginEnd="8dp"
            android:background="@drawable/generated_avatar_bg"
            android:contentDescription="@null"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:scaleType="centerInside"
            android:src="@drawable/voip_single_contact_avatar"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:loadProfileInitials="@{contact.First_Name}" />
        <!-- Name and Country Info -->
        <LinearLayout
            android:id="@+id/name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/avatar"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/nameTextView"
                style="@style/contact_name_list_cell_font"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="15sp"
                android:text='@{contact.First_Name+ " " + contact.Last_Name}' />

            <TextView
                android:id="@+id/countryTextView"
                style="@style/contact_organization_font"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="12sp"
                android:text="@{contact.country}" />
        </LinearLayout>

        <!-- Extension Info -->
        <TextView
            android:id="@+id/extensionTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="13dp"
            android:text="@{contact.PBX}"
            android:textColor="@color/black_color"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>