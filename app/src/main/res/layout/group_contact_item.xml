<layout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="position"
            type="Integer" />

        <variable
            name="selectionListViewModel"
            type="com.naminfo.cdot_vc.activities.main.viewmodels.ListTopBarViewModel" />

        <variable
            name="contactListViewModel"
            type="com.naminfo.cdot_vc.activities.main.contact.viewmodels.MasterContactsViewModel" />

        <variable
            name="contact"
            type="com.naminfo.cdot_vc.activities.main.contact.data.GroupSettingsContact" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="1dp"
        android:paddingEnd="8dp"
        android:paddingTop="0dp"
        android:paddingBottom="2dp">

        <ImageView
            android:id="@+id/avatar"
            android:layout_width="@dimen/contact_avatar_size"
            android:layout_height="@dimen/contact_avatar_size"
            android:layout_centerVertical="true"
            android:background="@drawable/generated_avatar_bg"
            android:contentDescription="@null"
            android:minWidth="48dp"
            android:minHeight="48dp"
            android:scaleType="centerInside"
            android:src="@drawable/voip_single_contact_avatar"
            app:layout_constraintBottom_toBottomOf="@+id/bcDetails"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
        <!-- Group Details Section -->
        <LinearLayout
            android:id="@+id/bcDetails"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_marginStart="8dp"
            app:layout_constraintVertical_bias="0"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/videoButton"
            app:layout_constraintStart_toEndOf="@id/avatar"
            app:layout_constraintTop_toTopOf="parent">
            <!-- Profile Image -->

            <TextView
                android:id="@+id/groupNameTextView"
                style="@style/contact_name_list_cell_font"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="0dp"
                android:layout_marginEnd="8dp"
                android:ellipsize="end"
                android:singleLine="true"
                android:text="Group Name" />
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content">
                <TextView
                    android:id="@+id/group_number"
                    style="@style/contact_organization_font"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:padding="4dp"
                    android:text="3500"
                    android:textSize="13sp"/>
                <TextView
                    android:id="@+id/call_type"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:layout_marginStart="4dp"

                    android:gravity="center"
                    android:padding="4dp"
                    android:text="call type"
                    android:textColor="@color/primary_dark_color"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    app:layout_constraintBottom_toBottomOf="parent"
                    app:layout_constraintEnd_toEndOf="@+id/audioButton"
                    app:layout_constraintStart_toStartOf="@id/infoButton"
                    app:layout_constraintTop_toBottomOf="@+id/infoButton" />
                <ImageView
                    android:id="@+id/infoButton"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_marginTop="2dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/info_button"
                    android:src="@drawable/info_black"
                    android:layout_gravity="center" />
            </LinearLayout>

        </LinearLayout>

        <!-- Info Button -->


        <!-- Video Button -->

        <!-- Audio Button -->
        <ImageView
            android:id="@+id/videoButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/round_orange_button_background"
            android:contentDescription="@string/video"
            android:padding="8dp"
            android:layout_marginEnd="8dp"
            android:src="@drawable/call_video_start"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/audioButton"
            app:layout_constraintTop_toTopOf="@+id/bcDetails" />

        <ImageView
            android:id="@+id/audioButton"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="@drawable/round_orange_button_background"
            android:contentDescription="@string/call_audio"
            android:padding="8dp"
            app:layout_constraintHorizontal_bias="1"
            android:src="@drawable/call_audio_start"
            app:layout_constraintBottom_toBottomOf="@+id/videoButton"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@+id/bcDetails"
            app:layout_constraintTop_toTopOf="@+id/videoButton" />



    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
