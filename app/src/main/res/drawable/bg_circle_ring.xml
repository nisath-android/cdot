<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <!-- No solid fill -->
    <solid android:color="@android:color/transparent" />

    <!-- Border/stroke for the ring -->
    <stroke
        android:width="4dp"
        android:color="@color/ptt_inactive_mic" />
</shape>
